version: 2

models:
  - name: stg_listings
    columns: 
      - name: listings_id
        tests:
          - unique
          - not_null

  - name: stg_hosts
    columns:
      - name: host_id
        tests:
          - unique
          - not_null
     